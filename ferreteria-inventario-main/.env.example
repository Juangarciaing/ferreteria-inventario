# ============================================
# VARIABLES DE ENTORNO - Sistema de Ferretería
# ============================================
# IMPORTANTE: Copia este archivo como .env y configura valores reales
# NO subas el archivo .env al repositorio

# ====== SEGURIDAD (REQUERIDO) ======
# Genera claves secretas seguras con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu_clave_secreta_muy_segura_aqui_minimo_32_caracteres
JWT_SECRET_KEY=otra_clave_secreta_diferente_para_jwt_minimo_32_caracteres

# ====== CONTRASEÑAS INICIALES (REQUERIDO) ======
# Cambia estas contraseñas inmediatamente después del primer deploy
ADMIN_DEFAULT_PASSWORD=cambiar_despues_del_primer_login
VENDEDOR_DEFAULT_PASSWORD=cambiar_despues_del_primer_login

# ====== BASE DE DATOS ======
DATABASE_URL=mysql+pymysql://root:root@localhost/ferreteria_db
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ferreteria_db
DB_USER=root
DB_PASSWORD=root

# ====== FLASK ======
FLASK_ENV=development
FLASK_DEBUG=True
API_PORT=5000
API_HOST=0.0.0.0

# ====== CORS (Orígenes permitidos, separados por comas) ======
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ====== JWT ======
JWT_EXPIRATION_HOURS=24

# ====== CACHÉ Y RATE LIMITING (Producción) ======
REDIS_URL=redis://localhost:6379/0

# ====== LOGGING ======
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# ====== BACKUPS ======
BACKUP_DIR=./backups
BACKUP_RETENTION_DAYS=30

# ====== TESTING (OPCIONAL) ======
# Solo necesario si ejecutas tests con credenciales personalizadas
# Si no se configuran, los tests generarán contraseñas aleatorias seguras
# TEST_ADMIN_EMAIL=admin@test.com
# TEST_ADMIN_PASSWORD=genera_con_secrets_token_hex
# TEST_USER_EMAIL=test@test.com
# TEST_USER_PASSWORD=genera_con_secrets_token_hex
